{
  "name": "@muralink/payments",
  "version": "0.1.0",
  "description": "Payment provider adapter — a stack-agnostic PaymentProvider interface plus an offline Mock and a Stripe adapter (client injected, no SDK dependency here). Charging a computed price goes through this seam so the concrete provider is swappable. Single-user core stays payment-agnostic; secret keys live server-side.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "verify": "tsx verify.ts"
  },
  "dependencies": {
    "@muralink/types": "*"
  },
  "license": "MIT"
}
