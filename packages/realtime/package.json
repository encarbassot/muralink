{
  "name": "@muralink/realtime",
  "version": "0.1.0",
  "description": "Generic WebSocket topic bus for the core server. No domain knowledge — any module can publish/subscribe to a topic it owns (e.g. a project id). Durability, auth semantics and message meaning are the caller's job.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "peerDependencies": {
    "express": ">=4"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  },
  "license": "MIT"
}
