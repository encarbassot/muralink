// orchesterCli entry — the master control surface for an elio instance.
// Boots/connects the daemon, then renders the Ink TUI.

import { render } from 'ink'
import { App } from './App'

render(<App />)
