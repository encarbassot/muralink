# notion — not yet implemented (scaffold only)

Adapter module over `notes` + `drive`.

- **Depends on:** `notes`, `drive`
- **Platforms (planned):** extension (content script), local-server
- **Interceptor (planned):** `readNotionPage`, page sync actions
- **Prior art:** `🧠/elio-modules/modules/notion` — `parsePageId(url)`,
  `parseTitle(rawTitle)`, `NotionPageProperties`, plus its `extension.ts` host entry.

Copy `modules/_template/` to start.
