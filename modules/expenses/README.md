# expenses — not yet implemented (scaffold only)

- **Depends on:** `contacts`, `@muralink/types` (`YMoney`)
- **Platforms (planned):** web, local-server

Build after `contacts` is solid. Copy `modules/_template/` to start.
