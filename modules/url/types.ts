// Types this module exposes. url builds entirely on the YUrl primitive from
// @muralink/types — re-exported here so consumers can import from the module.

export type { YUrl } from '@muralink/types'
