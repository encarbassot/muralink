# notes — not yet implemented (scaffold only)

- **Depends on:** `@muralink/types`
- **Platforms (planned):** web, local-server, extension
- **Prior art:** `🧠/LOCAL-SERVER` notes routes (markdown files + frontmatter);
  `🧠/elio-modules/modules/obsidian` (WebDAV vault) for the storage-integration split.

Copy `modules/_template/` to start.
