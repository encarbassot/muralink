# passwords — not yet implemented (scaffold only)

- **Depends on:** `url`, `@muralink/types` (`YPassword`)
- **Platforms (planned):** web, extension, local-server
- **Prior art:** none — `🧠/password-manager` is empty. Build fresh on `YPassword`
  (`{ hash, hint?, url? }`). Never store plaintext; only the hash.

Copy `modules/_template/` to start.
