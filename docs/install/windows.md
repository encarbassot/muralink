# Install — Windows

> Stub. The installer is not built yet (`platforms/server` is scaffold only).

## Planned

- **GUI installer** — a signed `.exe` wrapping Docker Desktop (WSL2 backend) for
  non-technical users.
- **One-line install** (PowerShell, Docker Desktop required) as an alternative.
- **What it installs:** nginx, Express API, git DB, file storage — all in Docker.

See [../self-hosting.md](../self-hosting.md).
