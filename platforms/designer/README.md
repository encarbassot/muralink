# designer — not yet implemented (scaffold only)

Visual block editor (was `PRESENTATION`). Drag blocks (= modules), draw typed
connections (= integrations or Interceptor actions). Configures the extension
overlay; includes a drag-and-connect entity modeler.

- **Stack:** React + Vite + TypeScript
- **Prior art:** `🧠/DESIGNER` has three sub-projects — consolidate on `cancas`
  (most mature canvas/node editor with panels, keyframes); drop `elioblog` and
  `elioputo`.

**Do not start until `packages/types`, `packages/core`, `url`, and `contacts`
are solid** (per bootstrap ordering). See [docs/deeplinks/designer.md](../../docs/deeplinks/designer.md).
